# OS
.DS_Store
Thumbs.db
desktop.ini

# Adobe
*.zxp
*.ccx

# Node (if ever added)
node_modules/
npm-debug.log*

# Build artifacts
dist/
build/
glow-native/build-cuda/
*.log

# Editor
.vscode/settings.json
.idea/
*.swp
*.swo

# Claude Code internals
.claude/

# Secrets / local config
.env
*.local

# Large reference/installer files
*.zip
*.exe
*.dmg
*.pkg

# Adobe AE SDK (vendored locally, not part of the repo)
AfterEffectsSDK_*/

# AE native plugin: SDK sample reference copy (read-only, not part of repo)
glow-native/ae-sample-ref/

# AE native plugin: build intermediates + outputs
glow-native/ae/x64/
glow-native/build-ae/
# PiPL .rc is generated from the .r during the build
glow-native/ae/*.rc

# Visual companion brainstorm sessions
.superpowers/

# native color engine build output
color-native/build/
